{
  "name": "@repo/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch --env-file=../../.env src/server.ts",
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "start": "node --env-file=../../.env dist/server.js",
    "test:integration": "tsx --env-file=../../.env --test test/bookings.concurrency.test.ts"
  },

  "dependencies": {
    "@repo/database": "*",
    "@repo/shared": "*",
    "fastify": "^5.2.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.7",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  }
}
